# Todo:

- **`not-done`**
- Todo component urgency badge
- Focus button (`/todos/___/focus`)
- Find some place instead of "FAB" for that... (on desktop)
- Make todo details done circle work too!
- Bucket/or_other tabs for todos (like Eitaa has...)
- Notes: { description, reason, action(next action, actionable thing we can do to start... physical what...), done, extraNotes/extra }
- Place, tools, contexts...
- Select todo process -> green, yellow, red todo: chosen, postponed, not-possible in time frame!

- **`done`**
- Remove "..." of todo component
- Show side by side todo details on desktop

- **`cancelled`**
- Modal for "editing", "new", "filter" (modal instead of page for desktop basically: "VIEWS")
